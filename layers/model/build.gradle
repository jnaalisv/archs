plugins {
    id 'java'
}

dependencies {
    compile(
            project(":persistence"),
            "org.springframework:spring-context:$springVersion",
            "org.springframework:spring-tx:$springVersion"
    )
}
